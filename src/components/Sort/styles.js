import styled from 'styled-components';

export const SortItems = styled.div`
  display: flex;
  align-items: center;
  justify-content: flex-end;
  margin: 1rem 0;

  span {
    margin: 0 1rem;
  }

  button {
    font-size: 16px;
    margin-left: .15rem;
    padding: .5rem .25rem;
  }
`;